// TODO: type the response from
// https://restcountries.com/v3.1/all?fields=name,capital,region,population,flags,cca3
//
// Hint: it's an array. Each item has roughly:
// {
//   name: { common: string; official: string; ... },
//   capital?: string[],          // sometimes missing!
//   region: string,
//   population: number,
//   flags: { png: string; svg: string; alt?: string },
//   cca3: string                 // 3-letter country code
// }
//
// Type only what you actually use. Watch the optional fields.
export type Country = {
  // fill this in
};
